# 银行流水检验工具 — 风险与漏洞分析报告

> 审查对象：[bankcheck.py](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py)
> 审查日期：2026-06-05

---

## 一、风险总览

| 等级 | 数量 | 说明 |
|------|------|------|
| 🔴 严重 | 3 | 数据丢失、逻辑错误、资源泄漏 |
| 🟠 高 | 4 | 性能瓶颈、数据完整性缺失、安全风险 |
| 🟡 中 | 4 | 健壮性不足、代码质量问题 |
| 🔵 低 | 3 | 可维护性、规范性问题 |

---

## 二、严重风险（🔴）

### 风险 1：处理出错的文件被误删，但提示信息声称已保留

**位置**：[bankcheck.py#L622-L630](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L622-L630)

**问题描述**：

当某个文件被识别为某银行（如"北京银行"），但在处理过程中抛出异常时，该文件会被加入 `error_files` 列表。然而后续的删除逻辑（第 624 行）仅判断 `filepath not in unprocessed_files`，而 `error_files` 中的文件并不在 `unprocessed_files` 中，因此**处理出错的文件同样会被删除**。

更严重的是，汇总提示信息（第 653-655 行）向用户显示"处理出错的文件（X 个，已保留）"，但实际上这些文件已经被删除。这造成了**数据丢失 + 误导用户**的双重问题。

**当前代码**：

```python
for filepath in excel_files:
    if filepath not in unprocessed_files:   # error_files 中的文件也不在 unprocessed_files 中
        try:
            os.remove(filepath)             # 会被删除
        except OSError as e:
            logger.error('删除文件「%s」失败: %s', filepath, e)
```

**解决方案**：

```python
error_file_set = {f for f, _ in error_files}
for filepath in excel_files:
    if filepath not in unprocessed_files and filepath not in error_file_set:
        try:
            os.remove(filepath)
        except OSError as e:
            logger.error('删除文件「%s」失败: %s', filepath, e)
```

---

### 风险 2：`get_subject()` 每次查询都重新打开整个查找表，导致严重性能问题和资源泄漏风险

**位置**：[bankcheck.py#L338-L371](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L338-L371)

**问题描述**：

`get_subject()` 函数每次被调用时，都会通过 `open_workbook_compat()` 重新打开并读取整个主体查找表 Excel 文件。对于一份包含 N 条交易记录的银行流水文件，该函数会被调用 N 次，意味着：

1. **性能灾难**：假设一个文件有 1000 条记录，查找表会被打开/关闭 1000 次。每次打开都需要读取磁盘、解析 XML（xlsx 本质是 zip+XML），极其缓慢。
2. **资源泄漏**：在 `try` 块中（第 361-362 行），匹配成功时先调用 `wb.close()` 再调用 `cleanup_temp_file(tmp_path)` 然后返回；而 `finally` 块（第 370 行）还会再次调用 `cleanup_temp_file(tmp_path)`。虽然 `cleanup_temp_file` 内部有 `os.path.exists` 检查不会报错，但 `wb.close()` 如果在 `try` 块中抛异常，`finally` 块不会关闭 workbook，造成文件句柄泄漏。
3. **`wb.close()` 在两个分支中重复调用**（第 361 行和第 365 行），不符合 Python 上下文管理最佳实践。

**解决方案**：

在处理文件前一次性加载查找表到内存字典，后续直接查字典：

```python
def load_lookup_table(lookup_file):
    """一次性加载主体查找表到内存，返回 {银行账号: 主体} 字典"""
    if not lookup_file or not os.path.exists(lookup_file):
        return {}
    wb, tmp_path = open_workbook_compat(lookup_file)
    try:
        ws = wb.active
        lookup = {}
        for row in ws.iter_rows(min_row=1, max_row=ws.max_row, min_col=1, max_col=2):
            account = row[1].value if len(row) > 1 else None
            subject = row[0].value
            if account is not None:
                lookup[str(account).strip()] = subject if subject else ''
        return lookup
    finally:
        wb.close()
        cleanup_temp_file(tmp_path)


def get_subject(bank_account, lookup_dict):
    """从预加载的字典中查找主体"""
    if not bank_account:
        return ''
    return lookup_dict.get(str(bank_account).strip(), '')
```

主流程中先加载，再传入字典：

```python
lookup_dict = load_lookup_table(lookup_file)
rows = processor(filepath, lookup_dict)
```

---

### 风险 3：唯一 ID 在快速循环中可能碰撞

**位置**：[bankcheck.py#L378-L382](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L378-L382)

**问题描述**：

`generate_unique_id()` 使用 `datetime.now().strftime('%Y%m%d%H%M%S%f')` 作为时间戳部分。`%f` 是微秒（6 位），但在紧凑循环中，连续调用可能返回相同的微秒值（取决于系统时钟精度）。虽然 UUID 部分提供了额外随机性，但时间戳部分在此场景下无实际碰撞防护价值，却增加了 ID 长度。

更重要的是：**整个项目没有对唯一 ID 做去重校验**。如果因某种原因（如系统时间回拨、并发执行）生成了重复 ID，下游系统会出现数据混淆。

**解决方案**：

```python
import uuid as _uuid

_used_ids = set()

def generate_unique_id():
    """生成全局唯一 ID，确保不重复"""
    while True:
        uid = _uuid.uuid4().hex
        if uid not in _used_ids:
            _used_ids.add(uid)
            return uid
```

如果业务上确实需要时间戳前缀以便排序，可改为：

```python
def generate_unique_id():
    ts = datetime.now().strftime('%Y%m%d%H%M%S')
    uid = _uuid.uuid4().hex[:8]
    candidate = f"{ts}_{uid}"
    if candidate not in _used_ids:
        _used_ids.add(candidate)
        return candidate
    return f"{ts}_{_uuid.uuid4().hex}"
```

---

## 三、高风险（🟠）

### 风险 4：临时文件命名可预测，存在竞态条件和冲突风险

**位置**：[bankcheck.py#L212-L214](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L212-L214)

**问题描述**：

`convert_xls_to_xlsx()` 使用 `{base_name}_converted.xlsx` 作为临时文件名，保存在系统临时目录中。该命名方式：

1. **可预测**：如果用户同时运行两个实例处理同名文件，会互相覆盖临时文件。
2. **未使用 `tempfile` 安全接口**：Python 的 `tempfile.mkstemp()` 可以生成唯一且安全的临时文件名，避免竞态条件。
3. **进程异常退出时临时文件残留**：虽然 `cleanup_temp_file` 在 `finally` 中调用，但如果进程被强制终止（kill -9），临时文件将残留在系统中。

**解决方案**：

```python
def convert_xls_to_xlsx(xls_path):
    import xlrd
    xls_book = xlrd.open_workbook(xls_path)
    wb = openpyxl.Workbook()
    wb.remove(wb.active)
    # ... 数据转换逻辑同原代码 ...

    fd, tmp_path = tempfile.mkstemp(suffix='.xlsx', prefix='bankcheck_')
    os.close(fd)
    wb.save(tmp_path)
    wb.close()
    xls_book.release_resources()
    return tmp_path
```

---

### 风险 5：缺乏 Excel 文件结构校验，异常格式会导致静默错误

**位置**：[bankcheck.py#L416-L478](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L416-L478)、[bankcheck.py#L481-L543](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L481-L543)

**问题描述**：

`process_beijing_bank()` 和 `process_east_asia_bank()` 都直接按硬编码的行号和列号读取数据，不进行任何结构校验：

1. **银行账号为空时仅打印 warning，但继续处理**：如果 B2（北京银行）或 B1（东亚银行）为空，所有记录的 `银行账号` 和 `主体` 都将为空/None，但程序不会中断或标记异常。
2. **不验证表头**：如果银行调整了流水格式（例如增减列、调整列顺序），程序会静默提取错误数据，用户无法察觉。
3. **`ws.max_row` 可能远大于实际数据行**：openpyxl 的 `max_row` 返回的是工作表中曾经使用过的最大行号，如果中间有空行，可能遍历大量空行。

**解决方案**：

```python
def process_beijing_bank(filepath, lookup_dict):
    wb, tmp_path = open_workbook_compat(filepath)
    try:
        ws = wb.active
        bank_account = ws['B2'].value
        if bank_account is None:
            raise ValueError(f'文件「{filepath}」B2 单元格为空，银行账号缺失，无法处理')

        # 可选：校验表头
        expected_header = {'A3': '交易日期', 'D3': '付款', 'E3': '收款'}
        for cell_ref, expected in expected_header.items():
            actual = ws[cell_ref].value
            if actual and str(actual).strip() != expected:
                logger.warning('表头校验不一致: %s 期望「%s」实际「%s」', cell_ref, expected, actual)

        subject = lookup_dict.get(str(bank_account).strip(), '')
        # ... 后续提取逻辑 ...
    finally:
        wb.close()
        cleanup_temp_file(tmp_path)
```

---

### 风险 6：无数据完整性校验——不验证提取记录数与源文件行数的一致性

**位置**：[bankcheck.py#L632-L641](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L632-L641)

**问题描述**：

处理完成后，程序直接将 `all_rows` 输出为总表，但**从不验证提取的记录总数是否与源文件中的实际交易行数一致**。以下场景会导致数据静默丢失：

- 某行 `trade_date is None` 导致被跳过（第 446 行），但该行可能实际有交易数据（日期列空白但金额列有值）
- Excel 中存在合并单元格，openpyxl 读取时只有左上角单元格有值，其余为 None
- 源文件中间存在空行，导致后续行被跳过

**解决方案**：

在处理器中增加计数和校验：

```python
def process_beijing_bank(filepath, lookup_dict):
    # ... 现有逻辑 ...
    skipped_rows = 0
    for row_idx in range(start_row, ws.max_row + 1):
        # 检查该行是否完全为空
        all_none = all(ws.cell(row=row_idx, column=c).value is None for c in range(1, 17))
        if all_none:
            continue

        trade_date = ws.cell(row=row_idx, column=2).value
        if trade_date is None:
            skipped_rows += 1
            logger.warning('第 %d 行交易日期为空但行内有数据，已跳过', row_idx)
            continue
        # ... 正常提取 ...

    if skipped_rows > 0:
        logger.warning('北京银行文件「%s」跳过了 %d 行无日期的记录', filepath, skipped_rows)
```

---

### 风险 7：`cli_askdirectory()` 接受用户输入无路径校验，存在路径遍历风险

**位置**：[bankcheck.py#L55-L61](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L55-L61)

**问题描述**：

在命令行模式下，`cli_askdirectory()` 直接将用户输入作为路径使用，仅检查 `os.path.isdir()`。虽然这是一个本地工具而非 Web 应用，但在自动化场景或脚本调用时，恶意构造的路径可能导致：

- 读取敏感目录中的文件
- 在意外位置创建"＋检验版"文件夹和删除文件

**解决方案**：

```python
def cli_askdirectory(title='请选择文件夹'):
    print(f'\n{title}')
    path = input('请输入文件夹路径: ').strip().strip('"').strip("'")
    path = os.path.abspath(path)
    if path and os.path.isdir(path):
        return path
    print(f'路径无效或不存在: {path}')
    return ''
```

---

## 四、中等风险（🟡）

### 风险 8：付款金额直接取负，无业务逻辑校验

**位置**：[bankcheck.py#L449-L450](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L449-L450)、[bankcheck.py#L514-L515](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L514-L515)

**问题描述**：

付款金额的处理逻辑为 `-abs(to_float(payment_val))`，即无条件取负。但如果源数据中付款金额本身已经是负数（某些银行流水格式中付款列直接以负数表示），取负后变为正数，导致付款变成收款。此外，没有对金额的合理范围做校验，异常大的数值可能表示数据读取错误。

**解决方案**：

```python
payment_raw = to_float(payment_val)
if payment_raw is not None:
    if payment_raw > 0:
        payment = -payment_raw
    elif payment_raw < 0:
        payment = payment_raw  # 已经是负数，保持原样
        logger.debug('第 %d 行付款金额本身为负数: %s', row_idx, payment_raw)
    else:
        payment = 0.0
else:
    payment = None
```

---

### 风险 9：银行识别仅依赖文件名前缀，容错性差

**位置**：[bankcheck.py#L274-L283](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L274-L283)

**问题描述**：

`identify_bank()` 仅通过文件名前缀判断银行类型，以下情况会导致识别失败：

- 文件名包含空格或特殊字符（如"北京银行_2024.xlsx"中的全角下划线）
- 文件名不以银行名开头但包含银行名（如"2024年北京银行流水.xlsx"）
- 文件名编码不一致（GBK vs UTF-8）

**解决方案**：

增加模糊匹配和文件内容辅助识别：

```python
import re

BANK_PATTERNS = {
    '北京银行': re.compile(r'北京银行'),
    '东亚银行': re.compile(r'东亚银行'),
}

def identify_bank(filepath):
    basename = os.path.basename(filepath)
    for bank_name, pattern in BANK_PATTERNS.items():
        if pattern.search(basename):
            return bank_name
    # 降级：尝试从文件内容中识别（如特定表头）
    return _identify_by_content(filepath)

def _identify_by_content(filepath):
    try:
        wb, tmp_path = open_workbook_compat(filepath)
        try:
            ws = wb.active
            # 北京银行特征：B2 为银行账号，第 3 行有特定表头
            # 东亚银行特征：B1 为银行账号
            b1 = ws['B1'].value
            b2 = ws['B2'].value
            if b1 and not b2:
                return '东亚银行'
            if b2 and not b1:
                return '北京银行'
        finally:
            wb.close()
            cleanup_temp_file(tmp_path)
    except Exception:
        pass
    return None
```

---

### 风险 10：`open_workbook_compat` 对 `.xls` 扩展名判断不够严谨

**位置**：[bankcheck.py#L223-L235](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L223-L235)

**问题描述**：

```python
if filepath.lower().endswith('.xls') and not filepath.lower().endswith('.xlsx'):
```

此判断逻辑虽然能正确区分 `.xls` 和 `.xlsx`，但如果文件扩展名与实际格式不匹配（如文件实际是 xlsx 格式但扩展名为 .xls），会导致 `xlrd` 读取失败。在实际业务中，银行导出的文件偶尔会出现此类问题。

**解决方案**：

增加文件格式自动检测（基于文件头 magic bytes）：

```python
def detect_excel_format(filepath):
    """通过文件头判断实际格式"""
    with open(filepath, 'rb') as f:
        header = f.read(8)
    # xlsx 实际是 zip 文件，以 PK 开头
    if header[:2] == b'PK':
        return 'xlsx'
    # xls 是 OLE2 格式，以 D0 CF 11 E0 开头
    if header[:4] == b'\xd0\xcf\x11\xe0':
        return 'xls'
    return 'unknown'
```

---

### 风险 11：总表输出直接覆盖，无备份机制

**位置**：[bankcheck.py#L639-L640](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L639-L640)

**问题描述**：

`df.to_excel(output_path, ...)` 会直接覆盖已存在的 `银行流水总表.xlsx`，无任何备份或确认。如果用户多次运行程序，之前的结果会被覆盖且无法恢复。

**解决方案**：

```python
if os.path.exists(output_path):
    backup_path = output_path.replace('.xlsx', f'_备份_{datetime.now().strftime("%Y%m%d%H%M%S")}.xlsx')
    shutil.move(output_path, backup_path)
    logger.info('已备份旧总表: %s', backup_path)
df.to_excel(output_path, index=False, engine='openpyxl')
```

---

## 五、低风险（🔵）

### 风险 12：两个银行处理器代码高度重复，违反 DRY 原则

**位置**：[bankcheck.py#L416-L478](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L416-L478)、[bankcheck.py#L481-L543](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L481-L543)

**问题描述**：

`process_beijing_bank()` 和 `process_east_asia_bank()` 的逻辑几乎完全相同，仅差异在起始行号和列映射。每新增一个银行都需要复制粘贴大量代码，极易引入不一致。

**解决方案**：

用配置驱动的方式统一处理器：

```python
BANK_CONFIGS = {
    '北京银行': {
        'account_cell': 'B2',
        'start_row': 4,
        'col_map': {
            '交易日期': 2, '付款': 4, '收款': 5,
            '摘要': 12, '对方户名': 7, '余额': 6, '交易流水号': 16,
        },
    },
    '东亚银行': {
        'account_cell': 'B1',
        'start_row': 5,
        'col_map': {
            '交易日期': 1, '付款': 4, '收款': 5,
            '摘要': 12, '对方户名': 12, '余额': 9, '交易流水号': 11,
        },
    },
}

def process_bank(filepath, lookup_dict, bank_name):
    config = BANK_CONFIGS[bank_name]
    wb, tmp_path = open_workbook_compat(filepath)
    try:
        ws = wb.active
        bank_account = ws[config['account_cell']].value
        if bank_account is None:
            raise ValueError(f'银行账号单元格 {config["account_cell"]} 为空')
        subject = lookup_dict.get(str(bank_account).strip(), '')
        rows = []
        for row_idx in range(config['start_row'], ws.max_row + 1):
            trade_date = ws.cell(row=row_idx, column=config['col_map']['交易日期']).value
            if trade_date is None:
                continue
            payment_val = ws.cell(row=row_idx, column=config['col_map']['付款']).value
            payment = -abs(to_float(payment_val)) if is_numeric(payment_val) else None
            receipt_val = ws.cell(row=row_idx, column=config['col_map']['收款']).value
            receipt = to_float(receipt_val) if is_numeric(receipt_val) else None
            rows.append({
                '唯一id': generate_unique_id(),
                '银行': bank_name,
                '银行账号': bank_account,
                '主体': subject,
                '交易日期': trade_date,
                '付款': payment,
                '收款': receipt,
                '摘要': ws.cell(row=row_idx, column=config['col_map']['摘要']).value,
                '对方户名': ws.cell(row=row_idx, column=config['col_map']['对方户名']).value,
                '余额': ws.cell(row=row_idx, column=config['col_map']['余额']).value,
                '交易流水号': ws.cell(row=row_idx, column=config['col_map']['交易流水号']).value,
            })
        return rows
    finally:
        wb.close()
        cleanup_temp_file(tmp_path)
```

---

### 风险 13：日志文件路径与可执行文件同目录，打包后可能无写权限

**位置**：[bankcheck.py#L130-L131](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L130-L131)

**问题描述**：

使用 PyInstaller 打包为 exe 后，`get_script_dir()` 返回的可能是 `C:\Program Files\` 等受保护目录，日志文件将无法写入。此外 `银行流水总表.xlsx` 也存在同样问题。

**解决方案**：

优先使用用户可写目录：

```python
def get_writable_dir():
    if getattr(sys, 'frozen', False):
        app_data = os.environ.get('APPDATA', os.path.expanduser('~'))
        app_dir = os.path.join(app_data, 'bankcheck')
        os.makedirs(app_dir, exist_ok=True)
        return app_dir
    return os.path.dirname(os.path.abspath(__file__))
```

---

### 风险 14：东亚银行的"摘要"和"对方户名"取自同一列 L，可能是设计疏漏

**位置**：[bankcheck.py#L520-L521](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02552/backend/bankcheck.py#L520-L521)

**问题描述**：

```python
summary = ws.cell(row=row_idx, column=12).value       # L 列
counterpart = ws.cell(row=row_idx, column=12).value    # L 列（同摘要）
```

东亚银行处理器的"摘要"和"对方户名"都取自 L 列，注释也标注"同摘要"。根据需求文档中的描述，这两者确实都指向 L 列。但如果未来东亚银行调整格式，这两个字段应该分别映射到不同列。建议添加明确注释说明这是需求规定而非代码错误，并确认业务方是否确实如此要求。

**解决方案**：

添加显式注释和配置验证：

```python
BANK_CONFIGS = {
    '东亚银行': {
        # ...
        'col_map': {
            '摘要': 12,
            '对方户名': 12,  # 需求规定：东亚银行摘要与对方户名在同一列（L列）
        },
    },
}
```

---

## 六、改进建议优先级排序

| 优先级 | 风险编号 | 改进项 | 预估工作量 |
|--------|---------|--------|-----------|
| P0 | 风险 1 | 修复错误文件误删逻辑 | 0.5h |
| P0 | 风险 2 | 查找表预加载 + 字典查询 | 1h |
| P1 | 风险 5 | 增加银行账号空值中断和表头校验 | 1h |
| P1 | 风险 6 | 增加提取记录数校验 | 1h |
| P1 | 风险 8 | 付款金额正负号逻辑修正 | 0.5h |
| P1 | 风险 12 | 配置驱动重构处理器 | 2h |
| P2 | 风险 3 | 唯一 ID 去重校验 | 0.5h |
| P2 | 风险 4 | 临时文件安全命名 | 0.5h |
| P2 | 风险 9 | 银行识别增强 | 1.5h |
| P2 | 风险 11 | 总表备份机制 | 0.5h |
| P3 | 风险 7 | 路径规范化 | 0.5h |
| P3 | 风险 10 | 文件格式自动检测 | 1h |
| P3 | 风险 13 | 可写目录适配 | 0.5h |
| P3 | 风险 14 | 字段映射确认与注释 | 0.5h |

---

## 七、总结

本工具在基础功能实现上是完整的，但在**数据安全性、健壮性和可维护性**方面存在较多需要改进的地方。其中最紧迫的问题是：

1. **错误文件被误删**（风险 1）—— 直接导致用户数据丢失
2. **查找表反复打开**（风险 2）—— 导致处理大文件时极度缓慢
3. **缺乏数据完整性校验**（风险 5、6）—— 可能静默产生错误结果

建议优先处理 P0 级别问题，再逐步推进 P1/P2 级别的改进。P3 级别问题可根据实际使用场景酌情处理。
